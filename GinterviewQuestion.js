//given a circular singly linked list, delete the nodes where the value is > 100
//a(6) --> b(500) --> c(40) => a -> c -> a
let list = [6, 500, 40];

function removeGreatest(start, threshold) {
    const nextNode = start.next;
}